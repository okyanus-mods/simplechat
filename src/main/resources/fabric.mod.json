{
  "schemaVersion": 1,
  "id": "simplechat",
  "version": "${version}",
  "name": "simplechat",
  "description": "Makes your server's chat simpler",
  "environment": "server",
  "depends": {
    "fabricloader": ">=0.4.8",
    "okyanus": ">=0.1.0"
  },
  "entrypoints": {
    "server": [
      "club.issizler.simplechat.SCMod::init"
    ]
  }
}
